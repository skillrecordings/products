export const productTitle = config.title

export const Message = () => (
  <p>
    Has sido eliminado de la lista de correo electrónico de {productTitle} y no
    recibirás más correos electrónicos al respecto.
  </p>
)

<h1 className="prose dark:prose-dark prose-lg">Suscripción cancelada.</h1>

<Message />

import config from '../config.json'

export default ({children}) => (
  <div className="prose dark:prose-dark prose-lg mx-auto py-24">{children}</div>
)
