export const productTitle = config.title

export const Message = () => (
  <>
    <p>
      Recibirás correos acerca de {productTitle}. Cada correo electrónico que
      enviemos tendrá un enlace para que canceles la suscripción en la parte
      inferior.
    </p>
    <p>¡No dudes en usarlo si no deseas recibir nuestros correos!</p>
  </>
)

<h1 className="prose dark:prose-dark prose-lg">¡Suscripción confirmada!</h1>

<Message />

import config from '../config.json'

export default ({children}) => (
  <div className="prose dark:prose-dark prose-lg mx-auto py-24">{children}</div>
)
