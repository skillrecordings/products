export const supportEmail = config.email

export const Message = () => (
  <p>
    Si no ves el mensaje después de unos minutos, revisa tu carpeta de spam y
    agrega <code>{supportEmail}</code> a tus contactos.
  </p>
)

<h1 className="prose dark:prose-dark prose-lg">Hay un último paso...</h1>

Por favor, revisa tu inbox. Te acabamos de enviar un correo. Deberás hacer clic en el enlace de confirmación para recibir más correos.

<Message />

import config from '../config.json'

export default ({children}) => (
  <div className="prose dark:prose-dark prose-lg mx-auto py-24">{children}</div>
)
