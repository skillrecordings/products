export const firstName = config.openGraph.profile.firstName
export const email = config.email

<h1 className="uppercase sm:text-base text-sm font-medium tracking-wide text-brand-pink-500">
  Just one last step and you are signed up...
</h1>

<h2 className="font-bold lg:text-3xl sm:text-2xl text-xl">
  Please check your inbox for an email that just got sent. You'll need to{' '}
  <span className="text-brand-pink-500">click the confirmation link</span> to
  receive any further emails.
</h2>

<p className="opacity-80 prose">
  If you don't see the email after a few minutes, you might check your spam
  folder or other filters and add <code>{email}</code> to your contacts.
</p>

import config from '../config'
import Layout from 'components/app/layout'
import Image from 'next/image'

export default ({children}) => (
  <Layout meta={{title: 'Confirm your subscription'}}>
    <div className="max-w-3xl text-center flex flex-col items-center justify-center space-y-5 mx-auto py-32 px-5">
      <Image src={'/email-notification.svg'} width={100} height={100} />
      {children}
      <div className="flex flex-col pt-6 text-sm opacity-80 text-gray-900">
        Thanks,
        <br />
        {firstName}
      </div>
    </div>
  </Layout>
)
