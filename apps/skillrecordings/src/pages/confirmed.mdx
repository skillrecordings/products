export const product = config.defaultTitle
export const name = config.author
export const firstName = config.openGraph.profile.firstName

<h1 className="uppercase sm:text-base text-sm font-medium tracking-wide text-brand-pink-500">
  Confirmed!
</h1>

<h2 className="font-bold lg:text-3xl sm:text-2xl text-xl">
  You'll receive emails from me ({name}) about {product}.
</h2>

<p className="opacity-80 prose">
  Every email we send will have a handy unsubscribe link at the bottom. Please
  feel free to use it if you don't want to receive emails about this!
</p>

import config from '../config'
import Layout from 'components/app/layout'
import Image from 'next/image'

export default ({children}) => {
  return (
    <Layout meta={{title: 'Subscription confirmed'}}>
      <div className="max-w-xl text-center space-y-5 mx-auto py-14">
        <Image src={'/email-confirmed.svg'} width={100} height={100} />
        {children}
        <div className="flex flex-col pt-6 text-sm opacity-80 text-gray-900">
          Thanks,
          <br />
          {firstName}
        </div>
      </div>
    </Layout>
  )
}
