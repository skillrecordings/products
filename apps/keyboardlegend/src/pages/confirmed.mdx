export const firstName = config.openGraph.profile.firstName
export const lastName = config.openGraph.profile.lastName
export const productTitle = config.defaultTitle

export const Message = () => (
  <>
    <p>
      You'll receive emails from me ({firstName} {lastName}) about{' '}
      {productTitle}.
    </p>
    <p>
      Every email I send about {productTitle} will have a handy unsubscribe link
      at the bottom. Please feel free to use it if you don't want to receive
      emails about this!
    </p>
  </>
)

# Confirmed!

<Message />

<div className="italic">
  Thanks,
  <br />
  {firstName}
</div>

import config from 'config'
import Layout from 'components/app/layout'

export default ({children}) => (
  <Layout>
    <div className="prose dark:prose-dark prose-lg mx-auto py-24">
      {children}
    </div>
  </Layout>
)
