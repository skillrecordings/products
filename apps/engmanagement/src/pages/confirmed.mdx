<h1 class="text-orange-200 uppercase font-semibold tracking-wide">
  Confirmed!
</h1>

<h2 class="font-medium text-3xl">
  You'll receive emails from me ({config.author}) about "{config.defaultTitle}
  ".
</h2>

<p class="opacity-80 prose prose-lg">
  Every email I send will have a handy unsubscribe link at the bottom. Please
  feel free to use it if you don't want to receive emails about this!
</p>

import config from '../../config'
import Layout from 'layouts'
import Signature from '../../public/images/signature.svg'
import EmailConfirmed from '../../public/images/email-confirmed.svg'
import Image from 'next/image'

export default ({children}) => {
  return (
    <Layout meta={{title: 'Subscription confirmed'}}>
      <div class="max-w-lg text-center space-y-5 mx-auto py-24">
        <Image src={EmailConfirmed} width={100} height={100} />
        {children}
        <div class="flex flex-col pt-6 text-gray-200 text-lg">
          <i>Thanks,</i>
          <div>
            <Image
              width={100}
              height={100}
              alt="Sarah's signature (made up by a designer)"
              aria-hidden="true"
              src={Signature}
            />
          </div>
        </div>
      </div>
    </Layout>
  )
}
