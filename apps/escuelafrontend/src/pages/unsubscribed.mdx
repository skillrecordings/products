export const firstName = config.openGraph.profile.firstName
export const productTitle = config.title

export const Message = () => (
  <p>
    You've been removed from the {productTitle} email list and won't receive any
    more emails about it.
  </p>
)

# Unsubscribed

<Message />

<div className="italic">
  Thanks,
  <br />
  {firstName}
</div>

import config from '../config.json'
import Layout from 'layouts'

export default ({children}) => (
  <Layout>
    <div className="prose dark:prose-dark prose-lg max-w-md mx-auto py-24">
      {children}
    </div>
  </Layout>
)
