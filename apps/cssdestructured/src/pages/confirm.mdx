export const firstName = config.openGraph.profile.firstName
export const email = config.email

<h1 className="font-display text-brand-brightYellow tracking-wider">
  conFirm yoUr sUbscRiptIon
</h1>

<h2 className="sm:text-xl text-lg py-4">
  Please <strong>click the confirmation link</strong> in an email that just got
  sent.
</h2>

<p className="max-w-sm opacity-75">
  If you don't see the email after a few minutes, you might check your spam
  folder or other filters and add{' '}
  <code className="text-sm bg-black bg-opacity-30 p-1 rounded-sm">{email}</code>{' '}
  to your contacts.
</p>

import config from '../config'
import Layout from 'components/app/layout'
import {ConfirmEmailSent} from 'components/images'

export default ({children}) => (
  <Layout meta={{title: 'Confirm your subscription'}}>
    <div className="max-w-3xl text-center flex flex-col items-center justify-center space-y-5 mx-auto px-5">
      <ConfirmEmailSent />
      {children}
      <div className="text-sm opacity-75 italic pt-4">
        Thanks,
        <br />
        {firstName}
      </div>
    </div>
  </Layout>
)
