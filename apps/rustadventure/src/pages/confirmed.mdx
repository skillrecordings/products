export const product = config.defaultTitle
export const name = config.author
export const firstName = config.openGraph.profile.firstName

<h1 class="uppercase sm:text-base text-sm font-medium tracking-wide text-brand-orange-600">
  Confirmed!
</h1>

<h2 class="font-bold lg:text-3xl sm:text-2xl text-xl">
  You'll receive emails from me ({name}) about Rust.
</h2>

<p class="opacity-80 prose">
  Every email we send will have a handy unsubscribe link at the bottom. Please
  feel free to use it if you don't want to receive emails about this!
</p>

import config from '../config'
import Layout from 'layouts'
import {FerrisWaving, Signature} from 'components/ferris'

export default ({children}) => {
  return (
    <Layout meta={{title: 'Subscription confirmed'}}>
      <div class="max-w-lg text-center space-y-5 mx-auto py-14">
        <div className="sm:w-40 w-32 mx-auto pb-4">
          <FerrisWaving />
        </div>
        {children}
        <div class="flex flex-col pt-6 text-sm opacity-80 text-gray-900">
          Thanks,
          <br />
          <Signature className="w-16 mx-auto pt-2" />
        </div>
      </div>
    </Layout>
  )
}
