export const firstName = config.openGraph.profile.firstName
export const email = config.email

<h1 class="uppercase sm:text-base text-sm font-medium tracking-wide text-brand-orange-600">
  Just one last step and you are subscribed...
</h1>

<h2 class="font-bold lg:text-3xl sm:text-2xl text-xl">
  Please check your inbox for an email that just got sent. You'll need to click
  the confirmation link to receive any further emails.
</h2>

<p class="opacity-80 prose">
  If you don't see the email after a few minutes, you might check your spam
  folder or other filters and add <code>{email}</code> to your contacts.
</p>

import config from '../config'
import Layout from 'layouts'
import {FerrisMailing, Signature} from 'components/ferris'

export default ({children}) => (
  <Layout meta={{title: 'Confirm your subscription'}}>
    <div class="max-w-2xl text-center space-y-5 mx-auto py-14">
      <div className="sm:w-40 w-32 mx-auto pb-4">
        <FerrisMailing />
      </div>
      {children}
      <div class="flex flex-col pt-6 text-sm opacity-80 text-gray-900">
        Thanks,
        <br />
        <Signature className="w-16 mx-auto pt-2" />
        {/* {firstName} */}
      </div>
    </div>
  </Layout>
)
